import { A as ReadContractConfig, C as GenericSigner, E as TransactionReceipt, P as WriteContractConfig, r as EIP712TypedData, s as Handle, w as SignerLifecycleCallbacks } from "../relayer-sdk.types-CGfXwKcB.js";
import { Abi, Address, ContractFunctionArgs, ContractFunctionName, ContractFunctionReturnType, EIP1193EventMap, EIP1193Events, EIP1193Provider, EIP1193Provider as EIP1193Provider$1, Hex, ProviderConnectInfo, ProviderMessage, ProviderRpcError } from "viem";
import { Signer, ethers } from "ethers";

//#region src/ethers/ethers-signer.d.ts
/**
 * Configuration for {@link EthersSigner}.
 *
 * Three variants:
 *
 * - **Browser** — `{ ethereum }`: pass the raw EIP-1193 provider (e.g. `window.ethereum`).
 *   A `BrowserProvider` is created internally and `subscribe()` works automatically.
 *
 * - **Node / direct signer** — `{ signer }`: pass an ethers `Signer` (e.g. `Wallet`).
 *   `subscribe()` is not available since there is no EIP-1193 provider.
 *
 * - **Read-only** — `{ provider }`: pass an ethers `Provider` for read-only contract calls.
 *   Signing and write operations will throw at runtime.
 */
type EthersSignerConfig = {
  ethereum: EIP1193Provider$1;
} | {
  signer: Signer;
} | {
  provider: ethers.Provider;
};
/**
 * GenericSigner backed by ethers.
 *
 * Accepts either a raw EIP-1193 provider (`{ ethereum }`) which creates a
 * `BrowserProvider` internally, or a `Signer` directly (`{ signer }`)
 * for Node.js scripts.
 *
 * @param config - {@link EthersSignerConfig}
 */
declare class EthersSigner implements GenericSigner {
  #private;
  constructor(config: EthersSignerConfig);
  getChainId(): Promise<number>;
  getAddress(): Promise<Address>;
  signTypedData(typedData: EIP712TypedData): Promise<Hex>;
  writeContract<const TAbi extends Abi | readonly unknown[], TFunctionName extends ContractFunctionName<TAbi, "nonpayable" | "payable">, const TArgs extends ContractFunctionArgs<TAbi, "nonpayable" | "payable", TFunctionName>>(config: WriteContractConfig<TAbi, TFunctionName, TArgs>): Promise<Hex>;
  readContract<const TAbi extends Abi | readonly unknown[], TFunctionName extends ContractFunctionName<TAbi, "pure" | "view">, const TArgs extends ContractFunctionArgs<TAbi, "pure" | "view", TFunctionName>>(config: ReadContractConfig<TAbi, TFunctionName, TArgs>): Promise<ContractFunctionReturnType<TAbi, "pure" | "view", TFunctionName, TArgs>>;
  getBlockTimestamp(): Promise<bigint>;
  waitForTransactionReceipt(hash: Hex): Promise<TransactionReceipt>;
  subscribe(callbacks: SignerLifecycleCallbacks): () => void;
}
//#endregion
//#region src/ethers/contracts.d.ts
interface EthersTransactionRequest {
  to: Address;
  data: Hex;
  gasLimit?: bigint;
  value?: bigint;
}
interface EthersTransactionResponse {
  hash: string;
}
interface EthersCallProvider {
  call(tx: EthersTransactionRequest): Promise<string>;
}
interface EthersTransactionSigner extends EthersCallProvider {
  sendTransaction(tx: EthersTransactionRequest): Promise<EthersTransactionResponse>;
}
declare function readConfidentialBalanceOfContract(provider: EthersCallProvider, tokenAddress: Address, userAddress: Address): Promise<unknown>;
declare function readUnderlyingTokenContract(provider: EthersCallProvider, wrapperAddress: Address): Promise<unknown>;
declare function readSupportsInterfaceContract(provider: EthersCallProvider, tokenAddress: Address, interfaceId: Address): Promise<unknown>;
declare function writeConfidentialTransferContract(signer: EthersTransactionSigner, tokenAddress: Address, to: Address, handle: Uint8Array, inputProof: Uint8Array): Promise<`0x${string}`>;
declare function writeUnwrapContract(signer: EthersTransactionSigner, encryptedErc20: Address, from: Address, to: Address, encryptedAmount: Uint8Array, inputProof: Uint8Array): Promise<`0x${string}`>;
declare function writeUnwrapFromBalanceContract(signer: EthersTransactionSigner, encryptedErc20: Address, from: Address, to: Address, encryptedBalance: Handle): Promise<`0x${string}`>;
declare function writeFinalizeUnwrapContract(signer: EthersTransactionSigner, wrapper: Address, burntAmount: Handle, burntAmountCleartext: bigint, decryptionProof: Hex): Promise<`0x${string}`>;
declare function writeSetOperatorContract(signer: EthersTransactionSigner, tokenAddress: Address, spender: Address, timestamp?: number): Promise<`0x${string}`>;
declare function writeWrapContract(signer: EthersTransactionSigner, wrapperAddress: Address, to: Address, amount: bigint): Promise<`0x${string}`>;
declare function readTokenPairsContract(provider: EthersCallProvider, registry: Address): Promise<unknown>;
declare function readTokenPairsLengthContract(provider: EthersCallProvider, registry: Address): Promise<unknown>;
declare function readTokenPairsSliceContract(provider: EthersCallProvider, registry: Address, fromIndex: bigint, toIndex: bigint): Promise<unknown>;
declare function readTokenPairContract(provider: EthersCallProvider, registry: Address, index: bigint): Promise<unknown>;
declare function readConfidentialTokenAddressContract(provider: EthersCallProvider, registry: Address, tokenAddress: Address): Promise<unknown>;
declare function readTokenAddressContract(provider: EthersCallProvider, registry: Address, confidentialTokenAddress: Address): Promise<unknown>;
declare function readIsConfidentialTokenValidContract(provider: EthersCallProvider, registry: Address, confidentialTokenAddress: Address): Promise<unknown>;
//#endregion
export { type EIP1193EventMap, type EIP1193Events, type EIP1193Provider, EthersSigner, type EthersSignerConfig, type ProviderConnectInfo, type ProviderMessage, ProviderRpcError, readConfidentialBalanceOfContract, readConfidentialTokenAddressContract, readIsConfidentialTokenValidContract, readSupportsInterfaceContract, readTokenAddressContract, readTokenPairContract, readTokenPairsContract, readTokenPairsLengthContract, readTokenPairsSliceContract, readUnderlyingTokenContract, writeConfidentialTransferContract, writeFinalizeUnwrapContract, writeSetOperatorContract, writeUnwrapContract, writeUnwrapFromBalanceContract, writeWrapContract };
//# sourceMappingURL=index.d.ts.map